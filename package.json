{
  "name": "devconnect-root",
  "version": "1.0.0",
  "description": "Root package for DevConnect deployment",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "npm run install && cd frontend && npm run build && cp -r build/* ../backend/public/",
    "start": "cd backend && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}